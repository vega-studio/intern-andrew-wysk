const quad = new QuadTree(new Bounds(0, 0, 1000, 1000));

const TWO_INTERSECT = [new Bounds(0, 0, 10, 10), new Bounds(5, 5, 10, 10)];

const TWO_MISS = [new Bounds(0, 0, 10, 10), new Bounds(50, 50, 10, 10)];
